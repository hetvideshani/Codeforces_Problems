#include <stdio.h>
int main()
{
    int t, i;
    scanf("%d", &t);

    for (i = 0; i < t; i++)
    {
        int n, j;
        scanf("%d", &n);

        char s[n + 1], p[n + 1];
        scanf("%d", s);

        for (j = 0; j < n; j++)
        {
        }
    }
}