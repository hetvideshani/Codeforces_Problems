#include <stdio.h>
int main()
{
    int t, i, j;
    scanf("%d", &t);

    for (i = 0; i < t; i++)
    {
        int a, b, c;
        scanf("%d%d", &a, &b);

        c = b - a;

        if (a >= (c + b / 2) || a > (c +))
    }
}