#include <stdio.h>
#include <string.h>

int main()
{
    int n, m, i, j;
    scanf("%d%d", &n, &m);

    char s[m][2];

    for (i = 0; i < m; i++)
    {
        for (j = 0; j < 2; j++)
        {
            scanf("%s", s);
        }
    }
}